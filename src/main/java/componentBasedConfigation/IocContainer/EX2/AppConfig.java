package componentBasedConfigation.IocContainer.EX2;

import org.springframework.context.annotation.ComponentScan;
import org.springframework.context.annotation.Configuration;

@Configuration
@ComponentScan(basePackages = "componentBasedConfigation.IocContainer.EX2")
public class AppConfig {

    // creating the bean using the annotation base ioc configuration
    // now specified the package to scan the beans
    // now we can use the annotation to create the beans
    // we can use the @Component annotation to create the beans
    // we can use the @Component annotation to create the beans


}
